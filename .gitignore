node_modules/
dist/
coverage/
data/
.env
.studio-token
*.log
.DS_Store

# CPU media connector token (if copied from vivijure deploy)
containers/tunnel.env
dev/module-fleet.pids
dev/module-fleet.env
.smoke/
.sync-staging/

# Edge TLS material (files/manual cert path writes real keys here; keep the dir, not the keys)
reverse-proxy/certs/*.pem
reverse-proxy/certs/*.key

# wrangler local dev state (if used)
.wrangler/
