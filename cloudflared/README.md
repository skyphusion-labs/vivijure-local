# Retired: cloudflared compose edge

The in-compose Cloudflare Tunnel profile (`tunnel`) was removed. Public HTTPS is
**Caddy** via `COMPOSE_PROFILES=edge`. See [docs/EDGE.md](../docs/EDGE.md).

Do not add tunnel credentials here for new installs.
