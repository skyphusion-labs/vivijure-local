# Runtime for containers/cast-image (local#269).
# Diffusers FLUX.2 Klein support needs a recent diffusers; pin loosely for homelab builds.
aiohttp==3.11.18
pillow==11.2.1
# Install torch+cu124 from the NVIDIA/pytorch index in the Dockerfile (not PyPI CPU wheels).
# transformers / accelerate / sentencepiece pulled in for Qwen3 text encoder on Klein 4B.
diffusers>=0.35.0
transformers>=4.51.0
accelerate>=1.6.0
sentencepiece>=0.2.0
protobuf>=5.29.0
safetensors>=0.5.0
huggingface-hub>=0.30.0
